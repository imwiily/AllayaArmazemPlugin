package com.example.armazem.util;

import org.bukkit.entity.Player;
import org.bukkit.Location;

public class PlotSquaredAPI {
    // Implementação dummy para fins de demonstração.
    public static boolean isInPlayerPlot(Player player, Location loc) {
        // Substitua pela lógica real utilizando a API do PlotSquared.
        return true;
    }
}
